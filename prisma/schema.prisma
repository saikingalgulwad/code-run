datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") // We'll store the connection string in .env
}

generator client {
  provider = "prisma-client-js"
}

model Feedback {
  id        Int      @id @default(autoincrement())
  text      String
  createdAt DateTime @default(now())
}
